from launch import LaunchDescription

# When creating a python launch file, this function must be defined or
# nothing will happen. It must match the name of this function exactly.
def generate_launch_description():
    ld = LaunchDescription()


    return ld
